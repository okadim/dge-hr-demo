services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - path: ./backend/.env
        required: false

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    environment:
      BACKEND_ORIGIN: http://backend:8000
    depends_on:
      - backend
